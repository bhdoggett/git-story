import React from "react";

const App: React.FC = () => {
  return (
    <div>
      <h1>Git Story (TypeScript + Vite + React)</h1>
      {/* Add your UI here */}
    </div>
  );
};

export default App;
