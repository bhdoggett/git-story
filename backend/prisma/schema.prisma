// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  githubId  String   @unique
  name      String?
  repos     Repo[]
}

model Repo {
  id           String   @id @default(uuid())
  user         User     @relation(fields: [userId], references: [id])
  userId       String
  githubRepoId String
  name         String
  narration    String[] // Each index is a chapter
  stories      Story[]
}

model Story {
  id            String   @id @default(uuid())
  repo          Repo     @relation(fields: [repoId], references: [id])
  repoId        String
  globalContext String?  // User's global context for the story
  createdAt     DateTime @default(now())
  chapters      Chapter[]
}

model Chapter {
  id          String   @id @default(uuid())
  story       Story    @relation(fields: [storyId], references: [id])
  storyId     String
  title       String?  // Generated title for the chapter
  summary     String   // AI-generated summary
  userNotes   String?  // User's custom notes
  commitShas  String[] // Array of commit SHAs in this chapter
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
